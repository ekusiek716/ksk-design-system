# iOS Simulator 操作確認 — 2026-09-25

環境: iPhone 17 Pro Simulator / iOS 26.5 / Expo Go SDK 57。
対象: `examples/native-device-review` が参照するDSソース。公開パッケージや実機の結果ではない。

## 修正前の観測

対象リビジョン: `906f830c278bcef8703bcc2f4179139eec9b58bb`。

- 起動画面: OS ios / 26.5、402 × 874、文字倍率1.00、safe area上62・下34を表示。
- 最初の接続はMetroがIPv6 localhostで待ち受ける一方、bundle URLが127.0.0.1となり失敗。IPv4でも待ち受けるサーバーへ変更後、Expo Goのホームからプロジェクトを開き直して起動した。途中の再読み込みでDevTools初期化エラーが出たが、開き直した後には再現しなかった。取得したソースマップのReact Native初期化モジュールは1系統であり、依存重複とは断定していない。
- シートの閉じるボタン→開くボタンを5回繰り返し、毎回「申込内容」が表示された。
- 項目1へ「確認用の入力」を設定。Simulatorのソフトキーボードを表示すると日本語キーボードが画面下部を覆い、保存ボタンとキーボードを閉じるボタンが見えなくなった。**keyboard確認は失敗**。
- キーボードを非表示にするとフッターが再表示された。
- アプリ内のダーク表示切替で背景・文字・ボタンの面が切り替わり、日本語の長文と操作ラベルが読めることを確認した。OSの文字倍率は1.00のまま。

証拠: このCodexタスクの2026-09-25「ソフトキーボード表示中のシートを確認」のSimulatorスクリーンショット・AX状態。画像では項目1の日本語入力とキーボードを確認でき、保存ボタンがキーボード下に隠れている。

## 修正中の観測

キーボードによって縮小した作業領域へパネルとフッターを収める変更後、項目1と3つのフッターボタンが日本語キーボードの上に表示された。キーボードを表示したまま保存ボタンを押し、ホームの「入力内容をこの画面内に保存しました。」を確認した。

この段階では項目6へフォーカスすると入力欄がフッターの裏に隠れるため、入力欄と本文の実座標を測定してスクロールする処理を追加した。最終変更後の操作確認は未完了で、合格の観測としては登録していない。

最終修正リビジョン: `12bc6297552df6605c98b6d3c00040830b399fd3`。全体チェック、Native型チェック、関連単体テスト、Web interaction 63件、a11y 773件、旧Radix回帰11件が通過。独立コードレビューの重大・中指摘は解消済み。これらは実OSの操作結果を代替しない。

証拠: このCodexタスクの「キーボードとフッターの位置を確認」「最終入力欄の表示位置を検証」「キーボード表示中の保存操作を確認」のSimulatorスクリーンショット・AX状態。

## 検証の限界

現時点で最大文字サイズ、VoiceOver、Android、実機の検証は未実施。開閉5回の成功だけで、ドラッグ・全snap位置を含むsheet-gesture検証の合格とはしていない。端末を回転させてもModalは縦向きを維持したため、横向きsafe areaの合格記録は作成していない。
